{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for UFC Panel",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default",
    "updater:default",
    "process:allow-restart"
  ]
}
